import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cafeteria_frontend',
  appName: 'cafeteria_frontend',
  webDir: 'build'
};

export default config;
